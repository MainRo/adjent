# Todo list

- Add url with host in config for the CLI to remove hardcoded url.
- in list operations, add a star - * - on the entry that is the active one, if any.
- during "adjent task create", also create an initial round.
